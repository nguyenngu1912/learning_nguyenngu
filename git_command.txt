- Tao user.name, user.email
git config --global user.name ""
git config --global user.email ""

- Check user.name, user.email
git config --global user.name
git config --global user.email

- Xoa  user.name, user.email
git config --global unset user.name
git config --global unset user.email

bo global de tao trong project hien tai

-Tao key ssh, Open git bash
ssh-keygen -t ed25519 -C "nguyenngu191202@gmail.com"           => tao ssh key
cat + "duong dan den public key"                                => lay public key
ssh -T git@github.com                                           => check ket noi

- Chuyen file tu working area -> index (staging area)
git add filename.type        => add 1 file cu the 
git add .                       => add tat ca file da thay doi
git add src/                    => add toan bo folder

- chuyen file tu index => local repo
git commit -m ""

- Git log 
git log                     => xem toan bo lich su
git log --oneline           => hien thi 1 dong/ 1 commit
git log -3                  => xem 3 commits gan nhat
git log --author=''         => xem commits cua nguoi cu the

- git remote -v                                         kiem tra ket noi 
git remote add <name> <url>                             ten remote thuong de la origin
git remote rename <old_name> <new_name>                 thay doi ten remote
git remote remove remote_name                           xoa remote

- Day code len github
git push -u origin main                                 chay lan dau tien, -u: thiet lap tracking